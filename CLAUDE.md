# Instrucciones para Claude Code — Agenda Inteligente de Proyectos

Este archivo define cómo debes trabajar en este repositorio. Léelo por completo antes de tocar código.

## 0. Reglas del espacio de trabajo (SIEMPRE se respetan, nunca se olvidan)

1. Siempre di lo que entendiste sobre lo que el usuario está pidiendo, antes de actuar.
2. Siempre haz preguntas para asegurar que entiendes lo que se necesita, y espera el visto bueno antes de avanzar en cambios grandes o ambiguos. Para cambios pequeños y obvios (typos, ajustes menores) puedes proceder directo.
3. Tu rol en este proyecto: desarrollador senior full-stack ayudando a Yue a construir este sistema, traduciendo requisitos en funcionalidad concreta.
4. Antes de construir algo nuevo de tamaño considerable (una fase completa, un módulo nuevo), presenta un plan corto de trabajo y espera confirmación.
5. Pide lo que necesites (credenciales, decisiones de negocio, aclaraciones) en vez de asumir.
6. Estas reglas se pueden ampliar más adelante; nunca deben olvidarse ni contradecirse por instrucciones futuras que no las mencionen explícitamente.

## 1. Qué es este proyecto

Sistema interno para una empresa (cliente: Bernardo — dirección — y su equipo; David es quien lidera el día a día de los proyectos como N2, ver sección 6) para:
- Llevar una agenda de entregables por proyecto, con fecha límite y % de avance.
- Ver histórico de avance (comparar sesión anterior vs. actual).
- Tener roles y permisos **por proyecto** (no globales): N1 (dirección), N2 (líder), N3 (colaborador interno), N4 (colaborador externo).
- Enviar recordatorios (por ahora solo dentro de la app; correo/WhatsApp quedan para fase futura).
- Servir como base que va a crecer con más módulos y usuarios — por eso el código debe estar bien estructurado, documentado y comentado desde el inicio.

Este es el **MVP (Fase 1 backend + Fase 2 frontend)**, ya ampliado con reuniones, minutas/acuerdos, dashboard ejecutivo, "Mi equipo" y una app móvil (PWA) — ver sección 6. Fases futuras que siguen sin aprobación y que NO debes construir a menos que se te pida explícitamente:
- Minutas de reunión con transcripción/IA (las minutas manuales, sin IA, ya existen — ver sección 6).
- Notificaciones por correo/WhatsApp.
- Asistente de voz con capacidad de ejecutar acciones ("agenda una reunión con David el jueves a las 3pm" y que la cree) — **aprobado el 2026-08-08** (Milestone B de la app móvil). El mecanismo y las primeras 2 acciones (`crear_entregable`, `actualizar_avance_entregable`) ya estaban construidos; el 2026-08-08 se agregaron las 4 acciones restantes del plan original (`crear_proyecto`, `agendar_reunion`, `asignar_rol`, `registrar_acuerdo`) — ver sección 6.

**Para quién es esto y hacia dónde va** (aclarado por Yue el 2026-08-13, para no perderlo de vista en fases futuras): Bernardo es una persona sumamente ocupada — no puede estar presente en todas las reuniones de sus N2, así que necesita poder ver notas de reuniones, avances de proyectos de su equipo, agendar reuniones, asignar tareas, etc., todo desde una **vista simple y con pocos clics** (la vista "Requiere tu atención"/Dashboard simplificado y "Resumen de mi equipo" ya construidas apuntan exactamente a esto). El asistente de voz es la otra mitad de esa promesa: una "secretaria" a la que se le pide algo por voz y lo hace, sin tocar la interfaz — crear/editar reuniones y notificarlas, crear proyectos y tareas, consultar avances, generar reportes (ver Milestone C en sección 6). Además, **todos** los que están bajo Bernardo van a usar el sistema y agregar contenido según su área — por ahora nada es financiero (ni Entregable ni Proyecto tienen campos de dinero), pero hay personas a cargo de Bernardo que manejan temas como inmuebles, compra de equipo, gastos de gasolina o monitoreo de paquetería. **No es alcance de ninguna tarea actual construir nada de eso** — es contexto para no inventar un modelo de datos financiero por adelantado ni sorprenderse si en una fase futura se pide agregarlo.

El documento de diseño completo (ERD, reglas de visibilidad, wireframes) está en `docs/diseno-fase0-agenda-inteligente.md`. Consúltalo antes de modificar el modelo de datos o las reglas de permisos.

## 2. Estructura del repositorio

```
agenda-backend/     API en FastAPI (Python). Ver su README.md.
agenda-frontend/    App web en React + Vite. Ver su README.md.
docs/                Documento de diseño de Fase 0.
```

## 3. Regla de oro de este sistema: permisos por proyecto

Toda la lógica de visibilidad vive centralizada en `agenda-backend/app/core/permissions.py`.
**Nunca dupliques lógica de permisos en un router o endpoint nuevo** — siempre reutiliza
`query_entregables_visibles`, `puede_ver_entregable`, `puede_editar_entregable`, etc., o
extiende ese módulo si necesitas una regla nueva.

Resumen de las reglas (no las cambies sin confirmarlo con Yue primero, ya que
fueron acordadas explícitamente con el cliente):
- **N1**: ve todo el proyecto.
- **N2**: ve solo a su equipo (usuarios donde `supervisor_id == N2.id` en ese proyecto), incluyendo entregables sensibles de su equipo.
- **N3 / N4**: ven solo sus propios entregables + los NO sensibles del proyecto.
- La sensibilidad (`sensible: bool`) se marca a nivel de **entregable individual**, no de proyecto completo.

## 4. Stack y convenciones

**Backend (FastAPI):**
- Python + FastAPI + SQLAlchemy + PostgreSQL + JWT.
- Todo el código, nombres de variables, comentarios y docstrings van **en español**, siguiendo el estilo ya usado en el repo (ver cualquier archivo en `app/`).
- Cada modelo, schema y router tiene un docstring de módulo explicando su propósito — mantén ese patrón.
- No agregues lógica de negocio directamente en los routers si ya existe o debería existir un servicio en `app/services/`.

**Frontend (React + Vite):**
- JSX en español para textos visibles al usuario; nombres de variables pueden ir en español o inglés siguiendo el patrón ya usado (mayormente español para mantener consistencia).
- Estilos con CSS plano usando las variables de `src/styles/tokens.css` — no introduzcas una librería de CSS/UI nueva sin antes preguntar.
- Cliente de API centralizado en `src/api/` — no hagas `fetch`/`axios` sueltos dentro de componentes.

**General:**
- Todo debe poder correr con Docker (`docker-compose.yml` en cada carpeta). Si agregas una dependencia nueva, actualiza también el Dockerfile/requirements.txt/package.json correspondiente.
- El proyecto eventualmente migra a AWS. Evita atarte a nada específico de "correr en mi laptop" (rutas absolutas, hardcodear `localhost`, etc.) — usa variables de entorno (`.env`).
- El túnel público estándar para compartir el proyecto es `agenda-demo.usw3` (CLI `devtunnel`) — **no** el de la pestaña "Ports" de VS Code, que causó una falla real por requerir un paso manual (marcar el puerto 8010 como Public) fácil de olvidar. Ver sección "Compartir el proyecto por internet" en `README.md` y el script `verificar-tunel.sh`.

## 5. Antes de escribir código en una tarea nueva

Sigue este flujo (coherente con las reglas del espacio de trabajo, sección 0):

1. Lee el requisito y repite en tus palabras qué entendiste que hay que construir.
2. Si el requisito toca el modelo de datos o las reglas de permisos, revisa primero `docs/diseno-fase0-agenda-inteligente.md` y `app/core/permissions.py` para no duplicar/contradecir lo ya definido.
3. Si es una tarea grande (nueva fase, nuevo módulo), propone un plan corto: qué archivos vas a tocar/crear, en qué orden.
4. Implementa.
5. Si tocaste el backend, valida que la app importa y arranca sin errores (`uvicorn app.main:app` o una prueba rápida de imports) antes de dar por terminada la tarea.
6. Si tocaste el frontend, corre `npm run build` para detectar errores antes de dar por terminada la tarea.

## 6. Estado actual (dónde vamos)

- ✅ Fase 0: diseño (ERD, endpoints, wireframes).
- ✅ Fase 1: backend base (modelos, auth, CRUD, permisos, entregables con histórico de avance, notificaciones in-app, resumen ejecutivo, script de datos de prueba). Incluye scheduler interno (APScheduler) que corre `generar_recordatorios()` automáticamente al arrancar y cada `HORAS_ENTRE_BARRIDOS_RECORDATORIOS` horas (default 6) — ya no depende de llamar `POST /admin/generar-recordatorios` a mano. Backend completo para el MVP.
- ✅ Fase 2: frontend — login, tablero de proyecto (semáforo + resumen), mis pendientes, creación/edición de entregables desde modal (`FormularioEntregable.jsx`), administración de equipo/roles por proyecto desde modal (`ModalEquipo.jsx`), histórico de avance con gráfica (`ModalHistorial.jsx`, usa `recharts`), notificaciones in-app con polling cada 60s (`ModalNotificaciones.jsx` + `AppLayout.jsx`). Estados de error visibles (no solo silenciosos) en Proyectos, Mis pendientes, Tablero de proyecto y actualización de avance.
- ✅ Calendario de entregables (`react-big-calendar`, componente reutilizable `CalendarioEntregables.jsx`): vista por proyecto (toggle Tabla/Calendario en `TableroProyecto.jsx`) y vista global (`/calendario`, página `CalendarioGlobal.jsx`). Permite arrastrar un entregable a otra fecha para reprogramarlo, solo si el usuario es N1/N2 en el proyecto correspondiente (reutiliza `PATCH /entregables/{id}`, sin cambios en el backend).
- ✅ Fase 2/3 completas para el MVP. Nada pendiente de esta lista por ahora.
- ✅ Chatbot de consulta (solo lectura, fase adelantada del roadmap original — confirmado con Yue antes de construirlo): página `Chatbot.jsx` (ruta `/chatbot`, link "Asistente" en el sidebar) → `POST /chatbot/consulta` → `app/services/chatbot.py`. El servicio arma el contexto reutilizando `query_entregables_visibles` (mismo filtro de permisos que el resto del sistema) y se lo pasa a un LLM **local** vía Ollama (`OLLAMA_URL`/`OLLAMA_MODELO` en `.env`, modelo probado: `mistral:7b-instruct-q4_0`), corriendo fuera de Docker en la máquina host — no sale a internet, para poder hacer demo dentro de la red del cliente sin depender de que abran el firewall a la API de Claude. El modelo nunca decide qué es visible ni toca la base de datos, solo redacta sobre datos ya filtrados.
- ✅ Reuniones: `app/routers/reuniones.py` + `app/models/reunion.py` (`Reunion`, `ReunionParticipante`). Cualquier participante del proyecto puede agendar una; visibilidad y edición con sus propias funciones en `permissions.py` (`query_reuniones_visibles`, `puede_ver_reunion`, `puede_editar_reunion` — N1/N2 o el organizador pueden editar/eliminar). Frontend: `ModalReunion.jsx`, usado desde `TableroProyecto.jsx` y `CalendarioGlobal.jsx`.
- ✅ Minutas y acuerdos: `app/routers/minutas.py` + `app/models/minuta.py` (`Minuta` 1:1 con `Reunion`, `AcuerdoMinuta` con `responsable_id`/`entregable_id`/`convertido`). Hereda permisos de la reunión (reutiliza `puede_ver_reunion`/`puede_editar_reunion`, sin reglas nuevas). `POST /acuerdos/{id}/convertir-a-entregable` convierte un acuerdo en un Entregable real llamando al mismo `crear_entregable_servicio` de siempre — mismas reglas de quién puede asignar a quién. Esto es la parte de minutas **sin IA/transcripción**; esa sigue siendo fase futura no aprobada. Frontend: `ModalMinuta.jsx` desde `TableroProyecto.jsx`.
- ✅ Dashboard ejecutivo: `GET /dashboard/resumen` (`app/routers/dashboard.py`) agrega avance/vencidos/próximos/cumplidos de todos los proyectos visibles + reuniones de los próximos 7 días + notificaciones no leídas, reutilizando `query_entregables_visibles`/`query_reuniones_visibles`. Página `Dashboard.jsx` (ruta `/`, antes era `Proyectos.jsx`), con `PanelResumenProyecto.jsx` como panel expandible por proyecto — ahí mismo los entregables se muestran **agrupados por persona** (no en listas separadas de equipo/entregables).
- ✅ "Mi equipo" (plantilla reutilizable): `app/routers/equipos.py` + `app/models/equipo_miembro.py`. Cada usuario guarda su propia lista de personas+rol (`GET/POST /mi-equipo`, `DELETE /mi-equipo/{usuario_id}`) y la aplica de un clic a un proyecto con `POST /proyectos/{id}/aplicar-mi-equipo` (solo N1/N2), que hace upsert directo sobre `usuario_proyecto_rol` sin reglas nuevas. Página `Equipo.jsx` (ruta `/equipo`) — no confundir con `ModalEquipo.jsx`, que administra los roles reales de un proyecto específico.
- ✅ Supervisor opcional al asignar N3/N4 (`asignar_rol_en_proyecto` en `proyectos.py`): si no se manda `supervisor_id`, queda automáticamente como supervisor quien hace la asignación (sea N1 o N2) — antes era obligatorio elegir un N2 ya existente, lo cual bloqueaba a alguien que acababa de crear un proyecto (y por tanto es N1 ahí) para agregar a su primer colaborador.
- ✅ Notificaciones ampliadas: además de `recordatorio_proximo/vencido` y `entregable_asignado`, ahora existe `avance_actualizado` — cada vez que alguien actualiza el % de avance de un entregable (no solo al llegar a 100%), se notifica al supervisor de esa persona en el proyecto, no solo a quien creó el entregable.
- ✅ App móvil — Milestone A (PWA instalable + responsive) completo el 2026-08-08: `vite-plugin-pwa` configurado en `vite.config.js` (manifest + service worker), íconos en `agenda-frontend/public/`, nav lateral colapsa a drawer con hamburguesa en pantallas ≤768px, tablas/calendario con scroll horizontal contenido. Se instala desde el navegador (Android/iOS) con "Agregar a pantalla de inicio", sin pasar por Play Store/App Store. Verificado el 2026-08-09 con Playwright en viewport móvil (375px) contra la app real: se encontró y arregló un bug de overflow horizontal en `TableroProyecto.jsx` — el encabezado (título + botones Tabla/Calendario/Administrar equipo/Nuevo entregable/Nueva reunión) no tenía `flex-wrap`, así que en pantallas angostas esa fila era más ancha que el viewport y arrastraba **toda la página** en scroll horizontal (no solo la tabla). Arreglado agregando `flex-wrap: wrap` a `.topbar` (`app.css`) y a los dos `div` de botones inline en `TableroProyecto.jsx`. El resto de las páginas (Dashboard, Proyectos, Mi equipo, Mis pendientes, Calendario global, Chatbot, el drawer, el modal del asistente de voz) ya se veían bien en móvil sin cambios.
- ✅ Milestone B (asistente de voz) — catálogo de acciones completo el 2026-08-08: `app/services/asistente/tools.py` ahora tiene las 6 acciones del plan original (`crear_entregable`, `actualizar_avance_entregable`, `crear_proyecto`, `agendar_reunion`, `asignar_rol`, `registrar_acuerdo`), todas reutilizando los mismos servicios/reglas de permisos de siempre, sin lógica nueva. `asignar_rol` solo permite cambiar el rol de alguien que YA participa en el proyecto (no agrega gente nueva por voz — resolver un nombre a un usuario_id sin exponer `GET /usuarios` global rompería la visibilidad N2/N3/N4). Se agregó `resolver_fecha_hora` en `resolucion.py`, con un extractor de hora por regex propio (`_extraer_hora`) en vez de confiar en el parseo combinado fecha+hora de `dateparser` en español, que en pruebas fallaba silenciosamente con frases como "de la mañana/tarde" o llegaba a interpretar mal el día. Frontend (`ModalAsistenteVoz.jsx`) no necesitó cambios: es genérico sobre cualquier tool del catálogo.
- ✅ Resumen de equipo multi-proyecto (2026-08-10): pestaña nueva "Resumen de mi equipo" en `/equipo` (queda por default; "Plantilla (Mi equipo)" es la pestaña existente, sin cambios). Para un N1/N2, agrega en un solo lugar — cruzando TODOS sus proyectos, agrupado por persona — equipo + entregables + reuniones, con acciones rápidas de cambiar rol/quitar inline y un botón "Administrar equipo de [proyecto]" que abre el `ModalEquipo` de siempre. Backend: `GET /equipo/resumen` (`app/routers/equipo_resumen.py` + `app/services/equipo_resumen.py`), reutiliza tal cual `listar_proyectos_visibles`/`listar_equipo_visible`/`query_entregables_visibles`/`query_reuniones_visibles` — **no se tocó `permissions.py`**, ninguna regla de visibilidad nueva. Importante: las reuniones que se muestran son exactamente las que ya eran visibles (para N2, solo donde él es organizador/invitado — no todas las de su equipo, verificado explícitamente con una prueba real antes de dar por terminada la tarea). Los controles de administrar (cambiar rol/quitar) se muestran según el rol del que ESTÁ VIENDO la pantalla en ese proyecto (`usuario.roles_por_proyecto`, mismo patrón que `TableroProyecto.jsx`), no según el rol de la persona mostrada.
- ✅ Skill de identidad visual personal (`.claude/skills/ui-ux-design/SKILL.md`, alcance solo este proyecto): paleta navy/teal y tipografía Sora/Inter ya definidas en `tokens.css` coinciden con el default del skill — no cambió nada visual existente, solo formaliza la convención y fija un piso de accesibilidad (contraste, foco de teclado visible, hover/focus/active distinguibles, `prefers-reduced-motion`) para código nuevo.
- ✅ Sidebar colapsable en desktop + acordeón en "Resumen de mi equipo" (2026-08-10): botón nuevo en `.app-topbar` (`topbar-collapse-btn`, mismo glifo "☰" que el hamburger móvil pero estado/CSS completamente separados) oculta/muestra el sidebar en pantallas ≥769px, con preferencia persistida en `localStorage` (`sidebar_colapsado`). Aplicando el piso de accesibilidad del skill de arriba: se agregó `prefers-reduced-motion` a la transición del sidebar (cubre también el drawer móvil existente) y un `:hover`/`:focus-visible` a `.btn--ghost` que no existía. En `ResumenEquipo.jsx`, cada persona ahora es un acordeón (un solo abierto a la vez, header con preview de "N proyectos · N vencidos" + ▲/▼) — mismo patrón visual que "Avance por proyecto" en `Dashboard.jsx`, pero implementado como `<button>` real (no `<div onClick>`) para que sea operable por teclado, corrigiendo en código nuevo un hueco de accesibilidad que sí tiene el patrón original de `Dashboard.jsx` (ese no se tocó, fuera de alcance). Verificado con Playwright: colapso persiste tras reload, el drawer móvil sigue funcionando sin regresión, el acordeón cierra la fila anterior al abrir otra, y Tab+Enter operan ambos controles nuevos.
- ✅ Fix + mejora sobre lo anterior (2026-08-10): el botón de colapsar sidebar quedaba centrado en el topbar (causa: `.app-topbar` usa `justify-content: space-between` pensado para 2 hijos; el tercer botón agregado rompía esa suposición) — se arregló agrupando ambos botones de "toggle" (`topbar-menu-btn`/`topbar-collapse-btn`) en un `<div>` envoltorio, y de paso se cambió su ocultamiento de `visibility` a `display: none` (más correcto para accesibilidad — saca el botón oculto del tab order). Además, "Resumen de mi equipo" ahora tiene un **segundo nivel de acordeón**: al expandir una persona, sus proyectos se ven de inmediato (nombre, rol, preview de entregables/vencidos, controles de administrar) pero los entregables/reuniones de cada proyecto quedan detrás de su propia flecha ▲/▼ (`proyectoExpandidoId`, mismo patrón de un-solo-abierto-a-la-vez, se resetea al cambiar de persona para que dos personas en el mismo proyecto no "hereden" el estado expandido entre sí). Nueva clase `.boton-desplegar` en `app.css` (distinta de `.list-inline--boton`: este botón es de ancho automático, comparte fila con los controles de administrar — no puede envolver toda la fila porque ya tiene un `<select>`/botones anidados, HTML inválido si fuera un solo `<button>` grande).
- ✅ Limpieza visual de "Resumen de mi equipo" (2026-08-10): cada fila de proyecto tenía select de rol + "Quitar" + "Administrar equipo de [proyecto]" siempre visibles — con varios proyectos por persona se veía como una pared de controles repetidos. Se quitaron el select y "Quitar" de la fila (quedan solo dentro de `ModalEquipo`, que ya los tenía) y se dejó un único botón "Administrar" por proyecto. Se eliminó el código ahora sin uso en `ResumenEquipo.jsx` (`handleCambiarRol`, `handleQuitar`, `ROLES`) — cero funcionalidad perdida, todo sigue disponible desde el modal.
- ✅ Vista Kanban en el tablero de proyecto (2026-08-10): tercer toggle "Kanban" junto a "Tabla"/"Calendario" en `TableroProyecto.jsx`, componente nuevo `KanbanEntregables.jsx` con 3 columnas = los 3 estados reales del entregable (`pendiente`/`en_progreso`/`cumplido`) — se descartó explícitamente inventar una columna "Bloqueados" o un concepto de "sprint" (el usuario pidió replicar un mockup tipo Jira, pero confirmó ceñirse al modelo de datos real). Cambio 100% aditivo y reversible: no se tocó el código de Tabla/Calendario, el default de `vista` sigue siendo `"tabla"`. Arrastrar una tarjeta entre columnas reusa el endpoint YA EXISTENTE `PATCH /entregables/{id}/avance` (`entregablesApi.actualizarAvance`), mapeando columna destino → `porcentaje_avance` objetivo (Pendiente=0/En progreso=50/Cumplido=100) — se eligió este camino y no el PATCH genérico (`entregablesApi.actualizar` con `estatus` directo) porque solo el primero permite que el propio responsable (no solo N1/N2) mueva su tarjeta y mantiene `estatus`/`porcentaje_avance` sincronizados automáticamente; el genérico los dejaría desincronizados. Drag-and-drop con HTML5 nativo, sin agregar ninguna librería (el proyecto no tenía ninguna de DnD). Sin cambios en backend ni en el modelo de datos. CSS nuevo en `app.css` reutilizando solo variables ya existentes en `tokens.css`.
- ✅ Vista simplificada del dashboard para N1 (2026-08-11): para un usuario que es N1 (o super admin) en TODOS sus proyectos, `Dashboard.jsx` ("/") muestra `DashboardSimplificado.jsx` ("Cards de atención") en vez del dashboard completo — 4 indicadores clave + tarjeta colapsable "Requiere tu atención" (vencidos → reuniones de hoy → próximos, en ese orden) + tarjeta colapsable "Tu equipo" (reutiliza tal cual `GET /equipo/resumen`, ordenado por vencidos, enlaza a `/equipo`) + un expansor "Ver todos los proyectos". Un usuario con rol N2/N3/N4 en cualquier proyecto sigue viendo el dashboard completo sin cambios — helper nuevo `usuarioEsN1EnTodo` en `src/utils/roles.js`. El dashboard completo se extrajo tal cual a `DashboardCompleto.jsx` (usado tanto para N2/N3/N4 como dentro del expansor de la vista N1) para no duplicar esa lógica. Backend: `DashboardOut` ahora incluye `entregables_atencion` (vencidos + próximos a vencer, con nombre/responsable/proyecto), calculado en el mismo loop que ya arma los conteos por proyecto en `GET /dashboard/resumen` — sin queries nuevas ni cambios a `permissions.py`.
- ✅ Notas en entregables, reuniones y minutas (2026-08-11): entidad nueva `Nota` (`app/models/nota.py`) con FKs nullable `entregable_id`/`reunion_id`/`minuta_id` (exactamente uno no nulo, con `CheckConstraint` de respaldo en BD además de la validación en el schema). Sigue al pie de la letra el patrón ya usado por `Minuta`/`AcuerdoMinuta`: la nota NUNCA calcula su propio permiso, siempre delega en `puede_ver_entregable`/`puede_editar_entregable` o `puede_ver_reunion`/`puede_editar_reunion` de `permissions.py` (para minuta, vía `nota.minuta.reunion`) — decidido explícitamente con el cliente: la nota hereda la visibilidad de su padre, nunca una regla de "equipo" nueva y más permisiva (así una nota en un entregable sensible o una reunión privada no se filtra a quien no podría ver el entregable/reunión en sí). Crear una nota requiere poder VER el padre; borrar requiere ser el autor o poder EDITAR el padre (N1/N2 del proyecto). Al crear una nota se notifica in-app (reutilizando `Notificacion`/`TipoNotificacion.otro`, sin tipo ni columna nueva) al responsable del entregable, o al organizador + invitados de la reunión/minuta, excluyendo siempre al autor. Frontend: componente reutilizable `SeccionNotas.jsx` (lista + textarea para agregar, botón "Eliminar" visible al autor y opcionalmente a quien administra el padre vía prop `puedeAdministrar`), integrado en `FormularioEntregable.jsx`, `ModalReunion.jsx` y `ModalMinuta.jsx` (solo en modo edición — un entregable/reunión nuevo sin guardar todavía no tiene notas, mismo criterio que Acuerdos con Minuta).
- ✅ Calendario personal vs. general (2026-08-11): toggle "Personal / General" en `CalendarioGlobal.jsx` (mismo patrón visual que el toggle Tabla/Calendario/Kanban de `TableroProyecto.jsx`), default "General" (comportamiento previo, sin regresión). "Personal" filtra en el cliente lo que ya llegó del backend — entregables con `responsable_id === usuario.id`, reuniones donde el usuario es organizador o aparece en `participantes` (`ReunionOut.participantes` ya lo traía) — sin ningún cambio de backend ni de `CalendarioEntregables.jsx`.
- ✅ Calendario de cumpleaños/eventos de empresa (2026-08-11): primer dato del sistema que NO es por proyecto — `EventoEmpresa` (`app/models/evento_empresa.py`, sin `proyecto_id`) con `tipo` (enum `cumpleanos`/`festivo`/`evento`) y `fecha`. `GET /eventos-empresa` (deliberadamente sin pasar por `permissions.py` — no hay alcance de proyecto que filtrar, documentado en el router para que no se lea como un descuido de la regla de oro) solo exige sesión válida, visible a cualquier usuario logueado. Recurrencia: se guarda la fecha completa tal como se carga, y el servicio calcula la "próxima ocurrencia" comparando mes/día contra hoy (solo para `cumpleanos`/`festivo`; un `evento` puntual se muestra en su fecha exacta) — 29 de febrero en año no bisiesto se corre al 1 de marzo. Carga manual vía `cargar_eventos_empresa.py ruta/archivo.csv|.xlsx` (columnas `nombre,tipo,fecha`), sin UI de carga (decidido con el cliente), idempotente por `(nombre, tipo)` — mismo estilo que `seed.py`. Requirió agregar `openpyxl` a `requirements.txt` (única dependencia nueva). Frontend: `CalendarioEntregables.jsx` gana una tercera capa de evento (`resource.tipo === "evento_empresa"`, no arrastrable, color `--color-teal-500`, ícono según tipo 🎂/📌/🎉); el toggle Personal/General de `CalendarioGlobal.jsx` se extendió a 3 opciones **excluyentes** (Personal/General/Empresa — no capas combinables, decidido con el cliente por simplicidad visual): al elegir "Empresa" se ocultan entregables/reuniones y solo se ven los eventos de empresa.
- ✅ Contraseña por defecto para usuarios reales (2026-08-12): `seed_usuarios_reales.py` y `reset_passwords_reales.py` ya no generan una contraseña aleatoria por persona (se perdía fácil, sin un lugar fijo donde consultarla) — ahora usan `PASSWORD_DEFECTO = "Demo1234!"` (misma que las cuentas demo), esperando que cada quien la cambie desde "Cambiar contraseña" la primera vez que entra. Se corrió el reseteo contra la base real de la Pi para los 7 usuarios existentes.
- ✅ Cumpleaños/eventos de empresa reales cargados (2026-08-12): `cargar_eventos_empresa.py` se hizo más flexible — detecta la fila de encabezado automáticamente (tolera filas de título antes, como traen los Excel exportados a mano) y reconoce alias de columna (`nombre`; `fecha`/`cumpleaños`/`cumpleanos`/`fecha de nacimiento`); si el archivo no trae columna `tipo` (ej. una lista que es solo de cumpleaños), acepta un tipo por defecto como segundo argumento de línea de comandos. Se cargaron los 130 cumpleaños reales de Presidencia. El archivo Excel/CSV con datos personales reales NO se sube a git (mismo criterio que `credenciales_reseteo.txt`) — se agregó `*.xlsx`/`*.csv` a `.gitignore`.
- ✅ Notificaciones automáticas de cumpleaños próximos (2026-08-12): `generar_recordatorios_cumpleanos` en `app/services/recordatorios.py` (mismo archivo/patrón que ya usan los recordatorios de entregables, corre en el mismo barrido del scheduler de `main.py` y también disparable a mano desde `POST /admin/generar-recordatorios`) notifica a **todos los usuarios activos** (no solo a alguien en particular — es informativo para todo el equipo) cuando el cumpleaños de alguien cargado en `EventoEmpresa` cae en 2 días, en 1 día, o es hoy. Reutiliza `proxima_ocurrencia` de `app/services/eventos_empresa.py` (se hizo pública para esto). Deduplicación por `(usuario, evento, día)` vía columna nueva `Notificacion.evento_empresa_id` (nullable) — como no hay Alembic todavía, se agregó con un `ALTER TABLE` manual además del cambio al modelo, ya que `create_all` solo crea tablas nuevas, no columnas nuevas en tablas existentes (documentado aquí para la próxima vez que se agregue una columna a una tabla ya creada). Frontend: la vista simplificada de N1 (`DashboardSimplificado.jsx`) muestra en "Requiere tu atención" los cumpleaños de ese mismo horizonte (hoy/mañana/en 2 días), consultando `GET /eventos-empresa` directo (no viene en `DashboardOut`).
- ✅ "Requiere tu atención" para todos los roles + tablero Kanban (2026-08-12): a petición de Yue, se quitó la distinción N1-puro/resto en `Dashboard.jsx` — **todos** los usuarios ven ahora lo que antes era `DashboardSimplificado.jsx` (incluida la tarjeta "Requiere tu atención"), no solo Bernardo. `DashboardCompleto.jsx` sigue existiendo tal cual, solo que ahora vive únicamente dentro del expansor "Ver todos los proyectos". Se eliminó `usuarioEsN1EnTodo`/`src/utils/roles.js` por quedar sin consumidores. Además, como experimento pedido explícitamente por Yue ("probar cómo se ve, revertible si no gusta"), "Requiere tu atención" pasó de lista colapsable a tablero Kanban de solo lectura — componente nuevo `KanbanAtencion.jsx` con 4 columnas (🔴 Vencidos / 🟡 Reuniones hoy / 🟡 Por vencer / 🎂 Cumpleaños), reutilizando tal cual las clases `.kanban-*` de `app.css` que ya existían para `KanbanEntregables.jsx` — a diferencia de ese, **sin drag-and-drop** (no hay una acción natural de "cambiar estatus" arrastrando una tarjeta de reunión o cumpleaños entre columnas heterogéneas). Sin cambios de backend ni de `permissions.py`: `GET /dashboard/resumen` ya calculaba `entregables_atencion` reutilizando `query_entregables_visibles`/`query_reuniones_visibles` para cualquier rol, el único gate que existía era de UI. **Nota sobre esta sesión:** se encontró que esta carpeta de Windows llevaba varias tareas (~15 commits: Kanban del tablero, notas, calendario de cumpleaños, la propia vista simplificada de N1) sin sincronizar desde la Pi — quedó corregido trayendo el estado completo de la Pi antes de este cambio (ver `[[project_windows-dev-sin-git]]`); vale la pena verificar el estado de sync al empezar sesiones futuras si hubo trabajo reciente hecho directo en la Pi.
- ✅ Usuarios y proyectos demo eliminados de la base real de la Pi (2026-08-12): a petición de Yue ("ya podemos trabajar solo con los usuarios reales"), se borraron los 8 usuarios de `seed.py`/prueba (`n1@demo.com`...`n4a@demo.com`, `prueba-delete@demo.com`) y sus 4 proyectos (Alfa/Beta/Gamma/Delta) de la base de datos real (no de un entorno de prueba aparte — este proyecto no tiene ambiente de staging, la Pi ES el entorno que usan los 7 usuarios reales). Antes de tocar nada se verificó que ningún dato real estuviera mezclado (conteos de entregables por responsable/reuniones por organizador limpios) — el único cruce era que David/Diana/José Francisco (reales) estaban agregados como N2 en "Proyecto Alfa" sin ningún entregable ahí, así que no se perdió nada real al borrar ese proyecto. El proyecto "prueba" (id 10, de David) NO se tocó — no es parte del seed demo. Ninguna FK en este esquema tiene `ON DELETE CASCADE` (todas son `NO ACTION`, ver salida de `information_schema.referential_constraints` si se necesita confirmar), así que el borrado se hizo a mano en orden de dependencias (acuerdos_minuta → historial_avance → notas → notificaciones → reunion_participantes → minutas → reuniones → entregables → usuario_proyecto_rol → equipo_miembros → proyectos → usuarios), dentro de una sola transacción `BEGIN/COMMIT` — si algo se me hubiera escapado, la falta de cascada hace que falle limpio en vez de arrastrar algo real. Se tomó un respaldo completo (`pg_dump`) antes, guardado en `backups/backup_antes_de_borrar_demo_20260812.sql` en la raíz del repo (Pi y copia en esta laptop) — la carpeta `backups/` y `*.sql` se agregaron a `.gitignore` porque el dump trae hashes de contraseña y datos reales. `seed.py` sigue en el repo tal cual (útil si se necesita volver a armar datos de prueba en otro entorno), simplemente ya no hay usuarios/proyectos demo cargados en la base de la Pi.
- ✅ Clic unificado en calendarios y Kanban (2026-08-12): antes, hacer clic en un entregable desde cualquier calendario o desde Kanban solo abría `ModalHistorial` (gráfica de avance, sin descripción/responsable/notas), mientras que un clic en una reunión ya abría su formulario completo — inconsistente. Se extrajo la gráfica+tabla de histórico a `HistorialAvance.jsx` (reutilizado por `ModalHistorial.jsx`, que ahora es un envoltorio delgado, y embebido como sección colapsable dentro de `FormularioEntregable.jsx`) y se cambiaron los `onEntregableClick` de `CalendarioEntregables` (en `TableroProyecto.jsx` y `CalendarioGlobal.jsx`) y de `KanbanEntregables` para abrir `FormularioEntregable` en vez de `ModalHistorial` — así el clic da SIEMPRE toda la información (campos + notas + histórico a un clic de distancia), igual que ya pasaba con reuniones. El botón "Ver histórico" independiente de la tabla se dejó tal cual (acceso rápido opcional, no se quitó nada). También se agregó `ModalEventoEmpresa.jsx` (detalle de solo lectura: nombre, tipo, fecha) y el prop `onEventoEmpresaClick` en `CalendarioEntregables.jsx`, ya que antes un clic en un cumpleaños/evento de empresa no hacía nada.
- ✅ Tema claro/oscuro + vista "Agenda" tipo lista semanal (2026-08-13): botón nuevo en la topbar (`useTema.js` + `AppLayout.jsx`) que respeta `prefers-color-scheme` del sistema hasta que el usuario lo toca manualmente (a partir de ahí ese override manda, guardado en `localStorage` como `tema`); el atributo `data-theme` se fija en un script inline pre-paint en `index.html` para no parpadear. `tokens.css` ganó un bloque `:root[data-theme="oscuro"]` que redefine las mismas variables `--color-*` (cero cambios en las 542 líneas de `app.css`, salvo 2 usos de la nueva variable `--color-heading` que separa el rol de "texto de encabezado" del de "fondo oscuro de chrome" que ya cumplía `--color-navy-900`). Por separado, la vista "Agenda" del calendario (la que ya caía automático en móvil) se reemplazó por un diseño tipo lista de Google Calendar — componente nuevo `VistaAgendaSemanal.jsx` (+ `utils/agendaSemanal.js` para la agrupación por semana/mes y `utils/eventosCalendario.js` para el color compartido con Mes/Semana/Día), usado desde `CalendarioEntregables.jsx` bypaseando por completo `react-big-calendar` cuando `vista === "agenda"` — Mes/Semana/Día no se tocaron. Solo muestra desde hoy hacia adelante (mínimo 4 semanas, tope 12), con banner de color sólido por mes (no se replicó el arte con marca de Google Calendar, por derechos de autor), círculo en el día actual, y sin drag-and-drop en esta vista (reprogramar sigue disponible con clic, que abre `FormularioEntregable`). Verificado en navegador real con Playwright (login contra la API real vía el túnel `agenda-demo`, no contra datos de prueba): tema oscuro persiste tras recargar y reacciona en vivo a cambios del SO mientras no hay override, sin errores de consola, `.card`/modales usan el color oscuro correcto (`#16232e`, confirmado con `getComputedStyle`), vista Agenda funciona en `/calendario` (General/Personal/Empresa) y en el toggle Calendario de `TableroProyecto.jsx` (sin `eventosEmpresa`), sin scroll horizontal en viewport móvil, clic en una pill abre el modal correcto. `npm run build` sin errores.
- ✅ Estilo unificado del toolbar de react-big-calendar (2026-08-13, mismo día que lo anterior): Yue notó que Mes/Semana/Día/Hoy-Ant-Sig se veían con un estilo distinto (el "segmentado" por defecto de la librería) al de Agenda (que ya usaba `.btn`/`.btn--primary`/`.btn--ghost` del resto de la app). Se sobrescribieron las clases nativas de `react-big-calendar` (`.rbc-toolbar`, `.rbc-btn-group`, `.rbc-toolbar button`, `.rbc-active`) en `app.css` para que tomen el mismo lenguaje visual — consistente ahora en las 4 vistas y en ambos temas, verificado en navegador real contra el túnel público.
- ✅ Kanban de equipo por supervisor en "Administrar equipo del proyecto" (2026-08-13): a petición de Yue, la lista plana de miembros en `ModalEquipo.jsx` se reemplazó por columnas Kanban agrupadas por supervisor, mismo patrón visual que "Tu equipo" del Dashboard (`KanbanSupervisores.jsx`) — pero acotado a un solo proyecto en vez de cruzar varios. Nuevo util `utils/equipoProyecto.js` (`agruparEquipoProyectoPorSupervisor`, forma plana de `GET /proyectos/:id/equipo`, distinta de la cross-proyecto que usa `equipoSupervisores.js`) y componente nuevo `KanbanEquipoProyecto.jsx`: una columna por supervisor con su propia tarjeta incluida como primer miembro de su columna (para no perder la opción de "Quitar" sobre esa persona) + una columna "Sin supervisor asignado" para quien no tiene supervisor ni reportes — así nadie desaparece de la vista de administración. Cada tarjeta es expandible (clic) y muestra los entregables/reuniones de esa persona **en este proyecto** (reutiliza `entregables`/`reuniones` que `TableroProyecto.jsx` ya tenía cargados, sin llamadas nuevas a la API). El formulario "Agregar o reasignar miembro" no cambió. `entregables`/`reuniones` son props opcionales (default `[]`) porque `ResumenEquipo.jsx` también abre `ModalEquipo` sin esos datos — ahí el Kanban funciona igual, solo sin detalle expandible.
- ✅ Fix: `color-scheme` faltante causaba que el modal se viera BLANCO en tema oscuro (2026-08-13, encontrado verificando lo anterior en navegador real): sin declarar `color-scheme` en `tokens.css`, Chromium aplicaba su propio re-theming a los `<select>`/`<input>` nativos del formulario dentro del modal — y visualmente también al contenedor del modal — sin que `getComputedStyle` lo reflejara (los valores de `--color-*` se leían correctamente pero no se pintaban así). Se agregó `color-scheme: light` al `:root` base y `color-scheme: dark` al bloque `:root[data-theme="oscuro"]` de `tokens.css`. Confirmado con capturas reales contra el túnel público: antes del fix el modal se veía blanco pese a que el resto de la página estaba oscura; después, modal y `<select>` quedan oscuros igual que el resto. Tema claro verificado sin regresión.
- ✅ Kanban también en "Equipo" (Resumen de mi equipo) y "Proyectos" (2026-08-13, misma sesión): a petición de Yue, el acordeón por persona de `ResumenEquipo.jsx` (pestaña "Resumen de mi equipo" en `/equipo`) se reemplazó por `KanbanSupervisores.jsx` — el mismo componente de "Tu equipo" del Dashboard, ahora extendido con dos props opcionales (`onAdministrar`/`puedeAdministrar`) para no perder el botón "Administrar" por proyecto ni el detalle de barra de avance/badge de estatus/badge "Sensible" que sí tenía el acordeón anterior; el uso en el Dashboard no pasa esas props y no cambia. `/proyectos` (`Proyectos.jsx`) ganó `KanbanProyectos.jsx` (nuevo) — columnas fijas N1/N2/N3/N4 según el rol que el usuario que ve la pantalla tiene en cada proyecto (confirmado con Yue, ante la falta de un campo de estatus propio en proyectos); Editar/Eliminar se conservan igual, gateados por `esN1DelProyecto`. Verificado en navegador real (ambos temas) contra el túnel público, sin errores de consola.
- ✅ Eliminar notificaciones + reemplazo de datos reales por Cubo/Suit/Agenda Inteligente (2026-08-13, sesión aparte de las de arriba): dos pedidos independientes de Yue. **(1)** Notificaciones: diagnóstico fue que "Urgente" no son filas de `Notificacion` (se calculan al vuelo en `useRequiereAtencion`, sin `id`, por diseño no tienen acción), pero "avisos" sí lo eran y el backend solo exponía `GET`/`PATCH` — nunca se podían borrar ni dejaban de listarse tras marcarse leídas. Se agregó `DELETE /notificaciones/{id}` (`app/routers/notificaciones.py`, mismo patrón de chequeo de dueño que el `PATCH` existente) + botón "Eliminar" en `ModalNotificaciones.jsx`; y por pedido de Yue, además se ocultan las leídas por default: `AppLayout.jsx` ahora pide `listar(true)` (solo no leídas) y el modal suma un toggle "Ver leídas" que trae bajo demanda `listar(false)` filtrado a `leida=true`. **(2)** Reemplazo de datos: se borraron los 4 proyectos que había (incluido "prueba", ya sin excepción) y todo lo que colgaba de ellos, y se crearon los 3 proyectos reales que David lidera hoy — **Cubo** (N3 Ana Garcia Avila), **Suit** (N3 Ivan Mora Garcia), **Agenda Inteligente** (N3 Juan Jose Flores Sedano, + N4 Jasso y Diana Chalini porque su rol ahí es dar retroalimentación, no entregar — funcionalmente N3/N4 ven lo mismo, es una etiqueta semántica) — Bernardo N1 y David N2 en los 3, confirmado con Yue que así debe ser en todo lo que David lidera. Se agendó la reunión "Revisión de avance — Agenda Inteligente" (19-ago-2026 11:00–12:00, organiza David) y el entregable "Avance de la agenda" (mismo proyecto, responsable Juan José, `fecha_entrega=2026-08-19` — el modelo `Entregable.fecha_entrega` es solo `Date`, sin hora, así que la hora quedó en la `descripcion` como texto). A diferencia de la limpieza de demo del 2026-08-12 (hecha a mano por SQL), esta vez quedaron dos **scripts reutilizables** en `agenda-backend/`: `limpiar_datos_proyectos.py` y `cargar_proyectos_reales.py`, ambos con modo dry-run por default (solo cuenta/describe) y `--ejecutar` para aplicar de verdad — se corrieron primero en dry-run y se le mostró el resultado a Yue antes de aplicar. Respaldo `pg_dump` previo en `backups/backup_antes_de_reemplazar_datos_reales_20260813.sql`. La limpieza NO toca usuarios, `equipo_miembros` ni `eventos_empresa`/cumpleaños — solo lo que cuelga de `proyectos`. **Hallazgo de infraestructura (aplica a cualquier cambio de frontend futuro):** el contenedor `web` NO tiene bind mount ni hot-reload — se construye con `docker compose build web` desde una imagen Docker que corre `vite build` + `vite preview` sobre el resultado ya compilado; a diferencia del contenedor `api` (sí monta `agenda-backend/` completo, por lo que ahí basta con reiniciar el contenedor tras copiar un archivo), un cambio de frontend copiado a la Pi por scp **no se refleja** hasta correr `docker compose build web && docker compose up -d web`. También se encontró que el Windows local había divergido de la Pi otra vez en `AppLayout.jsx` (trabajo sin commitear de la sesión de tema oscuro/sidebar) — se resolvió trayendo la versión de la Pi y reaplicando el cambio ahí en vez de sobreescribir; antes de cualquier `scp` hacia la Pi conviene diffear contra `git status`/el archivo real de la Pi, no asumir que la copia local está al día (ver `[[project_windows-dev-sin-git]]`).
- ✅ Asistente virtual — Milestone C (2026-08-13/14): a petición de Yue de repensar el asistente hacia "todo lo que el usuario puede hacer, el asistente lo debe poder hacer". Cuatro piezas:
  1. **Motor de LLM intercambiable Ollama↔Gemini** (`asistente_llm_proveedor` en `.env`/`config.py`, default `ollama`): `interprete.py` gana `_llamar_gemini`/`_llamar_llm` (dispatcher), solo para PROBAR el diseño del catálogo con un modelo más capaz — nunca para producción con datos reales (eso se mantiene 100% local). Se descartó explícitamente seudonimizar nombres reales para esta ronda (ver nota en el plan del milestone). **Aprendizajes de la prueba real con la API de Gemini (anotar para la próxima vez que se toque esto):** el modelo `gemini-2.0-flash` que se documenta en varios tutoriales ya no existe para llaves nuevas (`404 no longer available`); tampoco `gemini-2.5-flash` ni `gemini-1.5-flash`/`gemini-pro` (mismo 404, Google empuja hacia una "Interactions API" nueva que no se investigó). El que sí funciona hoy contra el endpoint clásico `generateContent` es **`gemini-flash-latest`** — quedó como default en `config.py`. Para diagnosticar esto se pidió primero `GET /v1beta/models?key=...` (confirma si la key es válida) y luego se probó cada nombre de modelo directo por curl — más rápido que adivinar.
  2. **Notificar a los invitados al agendar una reunión** (`services/reuniones.py`, `crear_reunion`): antes NO pasaba nada (a diferencia de crear un entregable, que sí notifica) — ahora cada invitado (menos el organizador) recibe una `Notificacion` tipo `otro`. Beneficia tanto a la UI manual como al asistente. Verificado end-to-end contra la Pi con una reunión de prueba (creada y borrada de inmediato).
  3. **4 tools nuevas de escritura** (`agregar_nota`, `editar_reunion`, `editar_entregable`, `editar_proyecto` en `tools.py`), mismo patrón `ToolSpec`/`resolver`/`ejecutar` que las 6 anteriores, envolviendo servicios que YA existían y YA tenían permisos (`crear_nota`, `actualizar_reunion`, `actualizar_entregable`, `actualizar_proyecto`) — no hubo que tocar `permissions.py`. `editar_reunion` agrega participantes en vez de reemplazar la lista (para que "agrega a X" no borre al resto de invitados que no se repitieron), y si el texto no resuelve a nadie NUEVO (ya invitado, o no identificado) pregunta en vez de confirmar a ciegas un cambio que no hace nada.
  4. **Consultas por voz** (`consultar_agenda`, única tool con `requiere_confirmacion=False`): en vez de construir lectura propia, reenvía a `chatbot.responder_pregunta` ya existente. `POST /asistente/interpretar` gana un tercer `tipo="respuesta"` (además de `propuesta`/`aclaracion`/`error`) que salta el paso de confirmar — `ModalAsistenteVoz.jsx` lo muestra igual que un resultado pero con ícono 💬 en vez de ✅. **Límite encontrado, no resuelto:** `chatbot.py` tiene su PROPIA llamada a Ollama hardcodeada (no pasa por el dispatcher de la Fase 1) — si se quiere que las consultas por voz también puedan usar Gemini para pruebas, falta extender el switch ahí también; no se hizo porque no estaba en el alcance acordado con Yue.

  **Bug real encontrado y arreglado durante las pruebas** (preexistente, no de esta tarea — afecta las 6 tools originales también, no solo las nuevas): `resolver_persona_en_equipo` (`resolucion.py`) matcheaba por substring libre, así que decir "Ana" resolvía silenciosamente a "Diana" (conten**ana** "ana" — di-**ana**) en vez de fallar o preguntar. Se encontró probando `editar_reunion` con nombres cortos (pedido explícito de Yue por privacidad al usar Gemini) — el "agregar a Ana" se veía como no-op porque en realidad agregó a Diana, que ya estaba invitada. Arreglado: ahora matchea por PALABRA completa (`any(palabra.startswith(normalizado) for palabra in nombre.split())`) en vez de substring en cualquier parte del nombre.

  **Estado de Ollama en la Pi (bloqueo de infraestructura, pendiente, no es un bug de código):** no hay ningún binario/servicio/contenedor de Ollama corriendo en esta Raspberry Pi — ni instalado siquiera (`ollama: command not found`). Además la Pi solo tiene 3.7GB RAM (1.9GB libres), posiblemente insuficiente para `mistral:7b-instruct-q4_0` cuantizado (~4-5GB). Esto significa que **el chatbot y el asistente de voz no funcionan hoy en la Pi** salvo que se cambie a Gemini a propósito — problema preexistente, no causado por este milestone. Pendiente de que Yue decida: reinstalar Ollama en la Pi (probar un modelo más chico dado el RAM), correrlo en otra máquina de la red, o evaluar Gemini/otro proveedor en la nube también para producción (rompiendo la premisa original de "100% local").

  Verificación: backend valida import/arranque en la Pi tras cada cambio; las 4 tools nuevas y el fix de nombres se probaron llamando a los `resolver`/`ejecutar` directo en el contenedor (sin pasar por HTTP, para no gastar cuota gratuita de Gemini de más) y también vía `/asistente/interpretar` real con Gemini. Todo dato/notificación de prueba se limpió de la base real al terminar. Al cerrar la tarea se borró el `.env` de prueba de la Pi (no existía antes) para que el proveedor vuelva al default `ollama` — la key de Gemini de Yue no quedó guardada en ningún archivo del repo ni de la Pi.
- ✅ Gemini pasa a ser el proveedor real (no solo de prueba) + seudonimización (2026-08-14, mismo día, sesión aparte): dado que Ollama no está instalado en la Pi (ver bullet anterior) y Yue decidió "por ahora solo vamos a usar Gemini, no el local de Ollama", esto ya no es una prueba puntual — es el motor real de producción, así que cambian las reglas: (1) **`chatbot.py` también pasa a usar el proveedor configurable** (antes tenía su propia llamada a Ollama hardcodeada, límite documentado en el bullet anterior) — se extrajo la llamada al LLM a un módulo nuevo compartido `app/services/llm_cliente.py` (`generar_texto(prompt, json_forzado=)`, dispatcher Ollama/Gemini), usado tanto por `interprete.py` (JSON forzado) como por `chatbot.py` (prosa); `interprete.py` perdió sus `_llamar_*` propios. (2) **Seudonimización real** en `app/services/llm_privacidad.py` (`construir_mapa`/`MapaSeudonimos.redactar`/`.restaurar`): antes de mandar cualquier texto a Gemini, sustituye nombres de personas y proyectos VISIBLES para ese usuario (mismas `listar_proyectos_visibles`/`listar_equipo_visible` de siempre) por placeholders (`PERSONA_1`, `PROYECTO_1`...), y traduce de vuelta los placeholders en lo que el modelo devuelve — aplica en `interpretar_instruccion` (ahora recibe `db`/`usuario`, no solo `texto` — `routers/asistente.py` actualizado) y en `responder_pregunta` (contexto + pregunta + respuesta). Solo corre cuando `asistente_llm_proveedor == "gemini"`; con Ollama no se toca nada (no hace falta, no sale de la red). **Límite documentado, aceptado a propósito:** protege la parte estructurada (quién/qué proyecto) pero NO contenido libre como el texto de una nota — no hay forma confiable de anonimizar prosa arbitraria sin arriesgar el sentido. (3) Se cambiaron los nombres de las personas en los `ejemplos` de `tools.py` (David/Ana/Diana/Bernardo → Carlos/Sofía/Lucía/Roberto, ficticios) para que no coincidan por casualidad con el equipo real — esos ejemplos son texto fijo del catálogo, no pasan por el mapa de seudónimos. **Verificado real:** se confirmó con el prompt crudo mandado a Gemini que "Juan"/"Agenda Inteligente" salen como `PERSONA_4`/`PROYECTO_3` y que el resultado final (`interpretar_instruccion`) trae los nombres reales restaurados; lo mismo confirmado para el prompt del chatbot (contexto completo con placeholders, cero nombres reales). La verificación completa de la respuesta restaurada del chatbot quedó bloqueada por rate-limit del free tier de Gemini (mismo mecanismo de restauración ya validado del lado del asistente, así que se dio por buena). El `.env` de la Pi ahora SÍ queda permanente con `ASISTENTE_LLM_PROVEEDOR=gemini` y la key real — a diferencia de la ronda anterior, esta vez es la config real, no se borra al terminar. **Nota de infraestructura de esta sesión:** el login de `devtunnel` en esta laptop expiró a medio trabajo (tenía una cuenta Microsoft, pero el túnel `agenda-ssh` exige la cuenta GitHub `yueLight12`) — se resolvió con `devtunnel user login -g -d` (login por device code: da una URL+código para autorizar desde CUALQUIER dispositivo con navegador, sin necesitar que esta laptop pueda llegar a GitHub) en vez de `--use-browser-auth` normal, que sí hubiera fallado por el bloqueo de red ya documentado en `[[project_windows-dev-sin-git]]`.
- ✅ Fix de calendario en modo oscuro + 3 kanbans de demo en "Tu equipo" (2026-08-14, sesión aparte, tras revisar la app real por el túnel público): dos pedidos chicos de Yue. **(1)** Las celdas de días fuera del mes actual en la vista Mes del calendario se veían blancas en tema oscuro — `react-big-calendar` nunca sobreescribe `.rbc-off-range-bg`/`.rbc-off-range` con las variables del sistema, así que dependían 100% de su CSS default (`#e6e6e6`/`#999999`, fijo, no reacciona a `data-theme`). Se agregó la regla en `app.css` usando `--color-pending-bg`/`--color-text-muted` — mismas variables que ya se usan en el resto de la app para "atenuado", y que en tema claro casi calcan el gris default de la librería (`#eef0f2` vs `#e6e6e6` original), así que no hay regresión visual ahí. `.rbc-today` (resaltado del día actual) se dejó igual — funciona "bien" en oscuro de pura casualidad, no estaba en el reporte de Yue. **(2)** "Tu equipo" en el Dashboard (`KanbanSupervisores.jsx`) ya estaba diseñado desde su creación para mostrar una columna por cada `supervisor_id` distinto que encuentre — no había que tocar nada de código para el pedido de "3 kanbans, uno por cada N2 (David/Jasso/Diana)". Se agregaron 4 usuarios de **demo** (`agenda-backend/agregar_colaboradores_demo.py`, mismo patrón dry-run/`--ejecutar`): Colaborador A/B supervisados por Jasso, Colaborador C/D supervisados por Diana, todos N3 en "Agenda Inteligente", con dominio `@demo.local` a propósito para que sean fáciles de identificar y borrar cuando Yue ya no los necesite para visualizar (instrucción de borrado documentada en el docstring del script). Con eso, David/Jasso/Diana ya aparecen como 3 columnas en "Tu equipo" sin ningún cambio de frontend ni backend — confirma que el componente escala solo con más datos.
- ✅ Jasso/Diana sin supervisor en "Agenda Inteligente" + voz para responder aclaraciones del asistente (2026-08-14, sesión aparte): dos pedidos de Yue tras revisar la app real. **(1)** El Kanban "Tu equipo" mostraba a Jasso y Diana (N4, colaboradores de retroalimentación) anidados como si reportaran a David — causa: `cargar_proyectos_reales.py` les había puesto `supervisor_id = David` explícitamente al darlos de alta. Yue aclaró la regla: alguien de un nivel de liderazgo equivalente que participa en un proyecto solo para dar retroalimentación/como oyente no debe aparecer como "parte del equipo" de otro N2 — su participación ya queda reflejada donde corresponde (rol N4 visible en "Administrar equipo del proyecto", invitado en la reunión) sin necesidad de una relación de supervisión. Se corrigió con un `UPDATE usuario_proyecto_rol SET supervisor_id = NULL` (SQL directo contra la Pi, no ameritaba un script nuevo por ser un cambio de una sola sentencia) para Jasso y Diana en ese proyecto — `KanbanSupervisores.jsx`/`agruparPorSupervisor` ya ignora silenciosamente filas sin `supervisor_id` (no genera columna ni membresía), así que no hizo falta tocar frontend ni backend. De paso se confirmó que el caso "un N3/N4 sin gente a su cargo, expandido en el Kanban, muestra sus proyectos con entregables/reuniones" (lo que David esperaba ver para Ana/Ivan/Juan) YA es el comportamiento existente de `KanbanSupervisores.jsx` (`EntregablesYReuniones`, mismo componente para cualquier rol que lo use — Dashboard, `/equipo`, `ModalEquipo`) — no fue necesario construir nada nuevo, solo faltaba que el dato de supervisor fuera correcto. **(2)** En el asistente de voz, cuando pide una aclaración de texto libre (ej. "No entendí la fecha... ¿puedes decirla de otra forma?"), `ModalAsistenteVoz.jsx` solo dejaba responder por teclado — se agregó un botón "🎙️ Responder por voz" junto al campo de texto, reusando el mismo pipeline de grabar→transcribir→enviar que ya existía para la instrucción inicial (`grabar()` generalizado para aceptar el callback de destino: `enviarTexto` o `responderAclaracion`). De paso, se corrigió que si fallaba el permiso de micrófono o la transcripción durante una aclaración, el modal reiniciaba TODA la conversación con el asistente (perdiendo `tool`/`parametros_llm`/aclaraciones ya dadas) — ahora regresa a la pregunta de aclaración en vez de al inicio, tanto desde el fallo de mic como desde el botón "Reintentar" de la pantalla de error. `npm run build` sin errores; desplegado en la Pi (`docker compose build web && up -d web`, el contenedor `web` no tiene hot-reload).
- ✅ Kanban "Tu equipo" por proyecto para quien no supervisa a nadie + notificaciones "Urgente" convertidas en reales (2026-08-14, misma sesión que el fix de Jasso/Diana): dos pedidos de Yue tras ver el Dashboard de David.
  1. **Kanban por proyecto para N3/N4 (u otro sin equipo a cargo):** `KanbanSupervisores.jsx` solo generaba columnas para gente que SÍ supervisa a alguien; para el resto mostraba el texto "No tienes equipo visible...". De paso se encontró un bug real en `agruparPorSupervisor` (`utils/equipoSupervisores.js`): cuando el supervisor de alguien no viene en la lista de `miembros` del que ve la pantalla (el caso normal para un N3/N4, que en `GET /equipo/resumen` solo se ve a sí mismo — confirmado con `listar_equipo_visible`), igual se creaba una columna fantasma con nombre `Usuario #<id>` en vez de caer en el mensaje vacío. Se arregló para que esas filas sin supervisor identificable se ignoren en el agrupado por supervisor, y `KanbanSupervisores` ahora tiene un fallback: si no hay ninguna columna de supervisor, muestra una columna por cada PROYECTO en el que la persona participa (nuevo componente `TarjetaProyectoPropio`, reutiliza `EntregablesYReuniones` con un nuevo prop `mostrarEncabezadoProyecto={false}` para no duplicar el nombre del proyecto que ya es el encabezado de la columna). Requirió pasar `usuarioActualId={usuario.id}` (de `useAuth()`) a `KanbanSupervisores` desde `DashboardSimplificado.jsx` y `ResumenEquipo.jsx`. Verificado directo contra la base real (sin pasar por HTTP): Ana (N3, solo en Cubo) ahora resuelve a una columna "Cubo" con sus propios entregables/reuniones, mientras que David (N2) sigue viendo su columna de siempre sin cambios.
  2. **Todas las notificaciones con marcar leída/eliminar:** antes existían dos mecanismos paralelos — "Aviso" (filas reales de `Notificacion`, con esas opciones) y "Urgente" (calculado en vivo cada vez que se abría el panel vía `useRequiereAtencion`, sin fila en BD, sin opciones). Confirmado con Yue el alcance antes de tocar el modelo de datos: los vencidos/próximos de OTRAS personas ahora notifican también al **supervisor directo** del responsable en ese proyecto (no a todo N1 de la cadena) — `generar_recordatorios()` en `app/services/recordatorios.py` calcula destinatarios como `{responsable_id} ∪ {supervisor_id si existe}`, con un mensaje distinto para el supervisor (menciona de quién es el entregable). Se agregó un generador nuevo, `generar_recordatorios_reuniones_hoy()` (no existía ningún recordatorio persistido de "esta reunión es hoy", solo el aviso de invitación al crearla) — notifica a organizador + invitados, deduplicado por día vía columna nueva `Notificacion.reunion_id` (nullable, mismo patrón que `evento_empresa_id`: como no hay Alembic, se agregó con `ALTER TABLE` manual + un `ALTER TYPE tiponotificacion ADD VALUE 'reunion_hoy'` para el enum de Postgres, documentado aquí para la próxima columna/valor de enum que se agregue a una tabla ya creada). Ambos generadores nuevos se llaman junto a los existentes en el barrido del scheduler (`main.py`) y en `POST /admin/generar-recordatorios`. Con esto, TODO pasa por `Notificacion` real: se retiró por completo la capa "Urgente" (`hooks/useRequiereAtencion.js` eliminado, `ModalNotificaciones.jsx` simplificado a una sola lista, `AppLayout.jsx` ya no hace el fetch duplicado de `dashboardApi.resumen()`/`eventosEmpresaApi.listar()` que antes hacía ese hook en cada pantalla). El badge de "Vencido" no leído se dejó en rojo (`--color-danger`) para conservar el mismo peso visual que tenía "Urgente" antes. **Límite aceptado a propósito, confirmado con Yue:** un N1 que no sea el supervisor directo de alguien (ej. Bernardo sobre el equipo de David) ya NO recibe notificación individual por cada vencido ajeno — sigue viendo el conteo agregado en el stat card "Vencidos" del Dashboard y el contador rojo por columna en el Kanban "Tu equipo", solo pierde el aviso individual en el panel de notificaciones.

  **Verificación:** `npm run build` sin errores; el contenedor `api` arrancó limpio tras la migración (`docker logs` sin excepciones) y se probaron ambos generadores nuevos con datos temporales reales (un entregable vencido de Juan José con supervisor David, una reunión de hoy con David de organizador y Juan José de invitado) creados, verificados con las notificaciones correctas generadas para cada destinatario, y borrados de inmediato — no quedó ningún dato de prueba en la base real. Desplegado en la Pi: migración SQL aplicada antes de reiniciar `api` (para que el modelo nuevo encontrara la columna/valor de enum ya creados), `docker compose build web && up -d web` para el frontend (el contenedor `web` no tiene hot-reload).
- ✅ Fix: Kanban con 1 sola columna se veía "aplanado" en móvil (2026-08-14, encontrado por Yue en el celular real justo tras lo anterior): `.kanban-board` tenía `min-width: 720px` fijo (y `min-width: 600px` en el breakpoint ≤768px de `app.css`) sin importar cuántas columnas hubiera — pensado para cuando SÍ hay 3 columnas, pero con una sola (el caso de David, o cualquier N3/N4 con el fallback "por proyecto" de arriba) igual forzaba ese ancho, empujando el badge de conteo y buena parte de la tarjeta fuera del viewport de un teléfono (~360-400px), y visualmente se veía como una lista plana con scroll en vez de una tarjeta Kanban. Se quitó el `min-width` fijo de `.kanban-board` (desktop y móvil) dejando que el ancho salga de `.kanban-column`'s propio `min-width` (220px desktop / 200px móvil, sin cambios) — con 1 columna ahora llena el 100% del contenedor sin overflow; con 2+ columnas que no caben, sigue disparando el scroll horizontal de siempre (`.kanban-responsive`, sin cambios), mismo comportamiento que ya tenía para el caso de varias columnas. `npm run build` sin errores, desplegado (`docker compose build web && up -d web`).
- ✅ "Tu equipo" rediseñado para que la granularidad de columnas sea consistente en cualquier nivel de la jerarquía (2026-08-14, mismo día, tras el fix de ancho móvil): Yue notó que el Kanban de David se veía como una sola columna con 3 filas adentro ("Ana/Iván/Juan"), mientras que el de Bernardo mostraba 3 columnas (David/Jasso/Diana) — pidió explícitamente que la regla fuera la misma para ambos, y que el sistema se ajustara solo conforme se agreguen personas/proyectos (no una solución fija para este caso puntual). Causa: el Kanban anterior agrupaba "una columna por cada `supervisor_id` encontrado en los datos" — eso da 3 columnas para Bernardo (David/Jasso/Diana son supervisor_id de alguien) pero para David da 1 sola columna (su propio id es el `supervisor_id` de Ana/Iván/Juan, así que ÉL se vuelve el encabezado con los 3 anidados adentro) — la regla no era "1 columna por persona a mi cargo" sino "1 columna por cada entidad que aparece como supervisor_id de alguien", y esas dos cosas coinciden para N1 pero no para N2. Se agregó `armarColumnasEquipo(miembros, usuarioActualId)` en `utils/equipoSupervisores.js`: toma el resultado de `agruparPorSupervisor` de siempre, pero la columna propia de quien ve la pantalla NUNCA se muestra tal cual — en vez de eso, cada uno de sus reportes directos se "promueve" a columna propia (con sus proyectos/entregables/reuniones mostrados directamente, sin necesidad de expandir), *a menos* que ese reporte YA tenga su propia columna por supervisar él mismo a alguien más visible (para no duplicarlo) — y si al final no queda ninguna columna (alguien sin nadie a cargo viendo su propio tablero), cae a una columna con su propio nombre y sus proyectos. Misma regla para cualquier rol, sin ramas especiales por N1/N2/N3/N4 — un futuro N2 con un N3 que a su vez supervise a alguien más (una jerarquía de 3 niveles) automáticamente mostraría esa columna intermedia expandible, sin tocar código de nuevo. `KanbanSupervisores.jsx`: `TarjetaSupervisor` (columnas expandibles, sin cambios) y `TarjetaColumnaPersona` (nueva, reemplaza a `TarjetaProyectoPropio` de la ronda anterior — incorpora ese caso como uno más de "columna de persona" en vez de un fallback aparte "por proyecto"). Verificado a mano contra los datos reales de la Pi (sin pasar por HTTP, revisando `GET /equipo/resumen` tal cual lo devuelve el backend): Bernardo sigue viendo exactamente 3 columnas David/Jasso/Diana sin cambio; David ahora ve 3 columnas Ana/Iván/Juan, cada una mostrando su proyecto directo; Ana (N3 sin nadie a su cargo) ve 1 columna con su propio nombre en vez del título de "Cubo" a secas de la versión anterior — más consistente con el resto. `npm run build` sin errores, desplegado (`docker compose build web && up -d web`).
- ✅ Fix de permisos: cualquier invitado puede editar la minuta (2026-08-14, encontrado por Yue al ver que a Juan José le fallaba "Guardar minuta" en la reunión real "Revisión de avance — Agenda Inteligente"): diagnóstico fue que `crear_o_actualizar_minuta`/`agregar_acuerdo`/`eliminar_acuerdo` en `app/services/minutas.py` usaban `puede_editar_reunion` (N1/N2 del proyecto o el organizador de esa reunión puntual) — Juan José es N3 y solo invitado (David es el organizador), así que el backend rechazaba el guardado con 403 y el modal mostraba "No se pudo guardar la minuta." (el frontend, `ModalMinuta.jsx`, nunca ocultó el formulario según permisos — siempre confió en que el backend fuera quien decidiera). Confirmado con Yue que el mismo bloqueo aplicaba a Jasso/Diana (invitados de solo retroalimentación, tampoco organizadores ni N1/N2) y que no era el comportamiento deseado. Se agregó `puede_editar_minuta` en `app/core/permissions.py` — regla **propia de la minuta**, no de la reunión: además de N1/N2/organizador, CUALQUIER invitado de la reunión puede crear/editar la minuta y sus acuerdos; la reunión en sí (título/fecha/participantes) sigue protegida solo por `puede_editar_reunion`, sin cambios ahí — decisión explícita de Yue para no perder el control de quién puede reprogramar/cancelar una reunión, solo relajar quién puede dejar notas de lo que se habló. `convertir_acuerdo_a_entregable` no se tocó (usa su propia regla vía `crear_entregable`, fuera de alcance de este pedido). Verificado directo contra la base real (sin escribir nada): Juan José/Jasso/Diana pasan `puede_editar_minuta` pero seguían sin pasar `puede_editar_reunion` (como se esperaba); David sigue pasando ambas. `docker restart` sobre `api` (bind mount, no necesita rebuild) arrancó limpio.
- ✅ Fix crítico de `GET /usuarios` + quitar etiquetas N1-N4 de las vistas + Kanban de "Tu equipo" por proyecto (2026-08-14, sesión aparte, con plan mode): tres piezas.
  1. **Bug crítico (P0), encontrado diagnosticando un reporte de Yue:** Juan José, usando "Administrar equipo" en un proyecto nuevo donde él mismo es Dirección (N1), veía "No tienes permiso... requiere N1" al intentar agregar a alguien — pese a SÍ tener el permiso (verificado con la función real contra la base: `_es_n1_en_algun_proyecto` daba `True`). Causa real: `GET /usuarios` devolvía **500 Internal Server Error** (no 403), porque los 4 usuarios de demo `@demo.local` creados la sesión anterior (`agregar_colaboradores_demo.py`) rompían la validación de email de Pydantic al serializar la lista completa (`.local` es un dominio reservado) — el frontend (`Equipo.jsx`/`ModalEquipo.jsx`) atrapaba CUALQUIER error de esa llamada con el mismo texto fijo "requiere N1", ocultando la causa real. **Alcance:** rompía "Agregar o reasignar miembro" para cualquier usuario en cualquier proyecto, no solo Juan José. Arreglado borrando los 4 usuarios de demo (ya habían cumplido su propósito de visualización, quedaba pendiente su borrado) — verificado con `GET /usuarios` real (token válido) pasando de 500 a 200. De paso, se cambió el mensaje fijo de error a uno neutral ("No se pudo cargar el listado de usuarios.") para no repetir un diagnóstico equivocado la próxima vez que ese endpoint falle por otra razón.
  2. **Aclarado con Yue, sin cambio de código:** por qué David no ve un proyecto que Juan José crea por su cuenta — es el diseño esperado (permisos 100% por proyecto, sin relación de supervisión global entre personas; el creador de un proyecto queda como única Dirección de ESE proyecto, `crear_proyecto` en `proyectos.py` no consulta ninguna tabla de "supervisor por defecto"). También se confirmó que Juan José SÍ puede agregar a David a su proyecto nuevo sin ningún bloqueo (ser Dirección de un solo proyecto, aunque sea uno propio y chico, ya desbloquea `GET /usuarios` global vía `_requerir_n1`, y `asignar_rol_en_proyecto` solo exige ser Dirección/Líder de ESE proyecto específico) — el bug del punto 1 fue lo único que lo bloqueaba en la práctica.
  3. **Etiquetas N1/N2/N3/N4 quitadas de las vistas:** nuevo `utils/rolLabels.js` (`ROL_LABELS`: N1→Dirección, N2→Líder, N3→Colaborador, N4→Externo — palabras ya acuñadas en `KanbanProyectos.jsx`, ahora centralizadas). Aplicado en los ~9 archivos que mostraban el código crudo al usuario: `Equipo.jsx`, `ModalEquipo.jsx` (selects de rol + "Plantilla (Mi equipo)"), `Proyectos.jsx` (texto de ayuda al crear), `KanbanProyectos.jsx` (encabezados de columna), `ModalReunion.jsx` (texto de ayuda de invitados + checkboxes), `KanbanEquipoProyecto.jsx` (badge de miembro), `FormularioEntregable.jsx`/`ModalMinuta.jsx` (selects de responsable), `PanelResumenProyecto.jsx`. **Nunca se tocó el valor lógico** — todas las comparaciones (`rol === "N2"`, filtros de `supervisoresPosibles`, permisos del backend) siguen comparando contra "N1"-"N4" tal cual; el cambio es 100% de texto mostrado.
  4. **Kanban "Tu equipo" — columnas por N2, contenido por proyecto:** a petición explícita de Yue ("en la vista de Bernardo solo mostrar los N2 y los proyectos con sus reuniones, no las personas N3"). Antes, `TarjetaSupervisor` listaba los REPORTES (personas) de cada N2, cada uno expandible con clic. Se reemplazó con `proyectosAgregados()` (nueva función en `utils/equipoSupervisores.js`): junta los `.proyectos` de TODOS los reportes de un supervisor en un solo listado por `proyecto_id` (entregables concatenados sin duplicar — cada uno tiene un solo responsable —, reuniones deduplicadas por `id` ya que una misma reunión puede repetirse si varios reportes están invitados). Resultado: **todas** las columnas del Kanban (sea la de un N2 con equipo agregado, o la de alguien sin equipo mostrando sus propios proyectos) terminan con la misma forma `{usuario_id, nombre, puesto, proyectos}` — se colapsó la distinción `tipo: "supervisor" | "persona"` de la sesión anterior en un solo componente `TarjetaColumna`, más simple que antes en vez de más complejo. Contenido de cada proyecto (entregables + reuniones) igual de completo que antes, solo agrupado distinto — nada de información se perdió, confirmado con Yue antes de implementar. De paso, se corrigió un gap chico de la sesión anterior: las columnas "de persona" no le pasaban `onAdministrar`/`puedeAdministrar` a `EntregablesYReuniones`, así que el botón "Administrar" nunca aparecía ahí — ya se pasa consistente en toda columna.
  **Verificación:** `npm run build` sin errores; el fix del punto 1 se probó con un token real de Juan José contra el endpoint HTTP real (no solo la función aislada), confirmando 500→200; los otros tres puntos son cambios de frontend puros, desplegados con `docker compose build web && up -d web`. Scripts de diagnóstico temporales borrados de la Pi y del contenedor al terminar.
- ✅ Etiquetas N1-N4 también quitadas de mensajes generados por el backend (2026-08-14, mismo día, tras revisión de Yue): la limpieza anterior solo cubrió el frontend — quedaron 3 mensajes del lado del backend que también llegan a la pantalla del usuario: la pregunta de aclaración del asistente de voz cuando no reconoce un rol dictado (`resolucion.py`, `resolver_rol`: "¿Es dirección (N1), líder (N2)...?" → "¿Es dirección, líder...?"), el resumen de confirmación al crear un proyecto por voz (`tools.py`, `_resolver_crear_proyecto`: "Quedarás como dirección (N1)" → "Quedarás como dirección"), y el detalle de error 403 de `GET/POST/PATCH /usuarios` (`usuarios.py`: "requiere rol N1" → "requiere rol de dirección"). No se tocó ningún otro texto de `tools.py` que menciona "N1-N4" porque son descripciones internas del catálogo de tools mandadas al LLM (Gemini/Ollama) para que entienda qué significa cada rol al interpretar una instrucción — nunca se renderizan en la UI, así que no aplica la regla de "sin etiquetas en las vistas". Verificado con grep exhaustivo en frontend Y backend que no quedó ningún mensaje de error/confirmación/pregunta restante con "N1"-"N4" crudo. Solo cambio de texto, ninguna lógica de permisos tocada — `docker restart` en `api` (bind mount, sin rebuild) arrancó limpio.
- ✅ "Tus proyectos" rediseñado: tarjeta por proyecto en vez de columnas por rol (2026-08-14, mismo día): Yue preguntó por qué esa vista agrupaba en 4 columnas fijas (Dirección/Líder/Colaborador/Externo) — respuesta: decisión del 2026-08-13 porque `Proyecto` no tiene un campo de estatus propio del que sacar columnas naturales, así que se usó el rol del viewer como sustituto. Yue pidió cambiarlo al mismo lenguaje visual que "Tu equipo": una tarjeta por PROYECTO (no por rol), que al desplegarse muestra a las personas del proyecto — confirmado con Yue que sea lista plana (sin sub-agrupar por supervisor, a diferencia de `KanbanEquipoProyecto.jsx` que sí lo hace dentro de "Administrar equipo"). Nuevo componente `KanbanMisProyectos.jsx` (reemplaza a `KanbanProyectos.jsx`, borrado por quedar sin uso): reutiliza tal cual el patrón `TarjetaMiembro`/`DetalleMiembro` de `KanbanEquipoProyecto.jsx` (persona expandible → sus entregables/reuniones EN ESE proyecto), pero sin el nivel de sub-columnas por supervisor. Cada columna es ahora un proyecto — encabezado con nombre (link a `/proyectos/:id`) + Editar/Eliminar si el viewer es Dirección ahí (sin cambio de comportamiento, mismo gate `esN1DelProyecto` de siempre) + tu rol en ese proyecto como badge chico (ya no como criterio de agrupación) + descripción + botón "Ver equipo ▼". El equipo/entregables/reuniones de cada proyecto se cargan **solo al desplegar esa columna por primera vez** (no las N×3 llamadas de golpe al abrir la página) — mismo patrón lazy-load que ya usa cada `TarjetaMiembro` para su propio detalle. `npm run build` sin errores, desplegado (`docker compose build web && up -d web`).
- ✅ Ocultar "Ver todos los proyectos" + alta de los 6 proyectos de Diana (2026-08-14, sesión aparte): dos pedidos de Yue tras revisar el Dashboard de Bernardo. **(1)** El expansor "Ver todos los proyectos ▼" de la tarjeta "Tu equipo" (`DashboardSimplificado.jsx`, mostraba `DashboardCompleto` inline) se ocultó a petición de Yue — se quitó el botón y el `useState` de `verTodo` que lo controlaba, pero `DashboardCompleto.jsx` **no se tocó ni se borró**, sigue existiendo por si se vuelve a enlazar más adelante. **(2)** Yue reportó ver "Colaborador A/B" bajo Jasso y "Colaborador C/D" bajo Diana en el Kanban "Tu equipo" — se verificó contra la base real de la Pi (solo lectura) que esos 4 usuarios de demo ya NO existen (se habían borrado como parte del fix de `GET /usuarios` documentado arriba); la captura de Yue era una vista vieja/cacheada del navegador, confirmado con una recarga fuerte de su parte — no hizo falta ningún cambio de código ni de datos. **(3)** Alta de 6 proyectos reales que Diana lidera (temas que maneja para Bernardo — inmuebles/gastos, ver nota de la sección 1 sobre áreas no financieras de otras personas del equipo): Despachos, "Casa, personales y oficinas", Suits, OCESA/Socio plus, RED, Centro comunitario — mismo patrón de roles que Cubo/Suit/Agenda Inteligente con David: **Bernardo Dirección (N1) + Diana Líder (N2)**, sin nadie más asignado todavía (ella agrega su propio equipo después desde "Administrar equipo"). Script nuevo `agenda-backend/cargar_proyectos_diana.py` (mismo patrón dry-run/`--ejecutar`/idempotente que `cargar_proyectos_reales.py`, sin reunión ni entregable porque no se pidió ninguno) — corrido primero en dry-run, plan mostrado a Yue, confirmado, y aplicado con `--ejecutar`. **Nota de infraestructura de esta sesión:** el túnel SSH privado `agenda-ssh` se cayó a medio trabajo (`devtunnel show agenda-ssh` marcaba `Host connections: 0` — nadie corriendo `devtunnel host agenda-ssh` del lado de la Pi, posible caída del proceso o del servicio systemd) y bloqueó todo despliegue hasta que Yue lo reinició del lado de la Pi; del lado de esta laptop también hubo que reiniciar `devtunnel connect agenda-ssh` (había quedado con un proceso zombie apuntando a un túnel que ya no aceptaba conexión, error 404 "tunnel or port not found") — contradice la nota de la sesión anterior de que `agenda-ssh` ya corría como servicio systemd estable; vale la pena confirmar de nuevo con Yue el estado real de ese servicio en la Pi la próxima vez que se toque infraestructura. `npm run build` sin errores, desplegado (`docker compose build web && up -d web`).
- ✅ Fix: Jasso y Diana desaparecían del Kanban "Tu equipo" de Bernardo (2026-08-14, mismo día, encontrado por Yue justo tras lo anterior): al confirmar que los colaboradores demo ya no existían, Yue notó que el Kanban de Bernardo pasó de 3 columnas (David/Jasso/Diana) a mostrar solo David — bug real, no solo la vista vieja/cacheada que se había asumido. Causa: `armarColumnasEquipo` (`utils/equipoSupervisores.js`) solo generaba columna para alguien si (a) supervisaba a alguien, o (b) era reporte directo de quien ve la pantalla — Jasso y Diana no cumplen ninguna de las dos (no supervisan a nadie desde que se les quitaron los colaboradores de demo / se les puso `supervisor_id = NULL` la sesión pasada, y tampoco tienen a Bernardo como su supervisor directo en ningún proyecto), así que simplemente no entraban en ninguna columna — se perdían del todo en vez de mostrarse vacíos, que es lo que Yue pidió desde el inicio de esta sesión ("aun quiero que esten disponibles en la vista de bernardo aunque no tengan nada"). Se generalizó la regla: además de las columnas por supervisor y las de reportes directos del viewer, **cualquier persona visible para el viewer que no quede cubierta por ninguna de esas dos** (ni como supervisor, ni como reporte de alguien) ahora obtiene su propia columna con sus propios proyectos — vacía si no tiene ninguno. Esto es genérico (no hay ninguna rama especial para Jasso/Diana por nombre) — aplica igual a cualquier persona futura que quede en esa misma situación de "nivel par, sin relación de supervisión en ningún sentido". Con esto, Jasso vuelve a aparecer (columna vacía o con su participación en Agenda Inteligente) y Diana aparece con sus 6 proyectos nuevos agregados de la tarea anterior. `npm run build` sin errores, desplegado (`docker compose build web && up -d web`).
- ✅ Fix crítico: no se podía eliminar un proyecto (o una reunión) con minutas o notas (2026-08-14, mismo día, encontrado por Yue al intentar borrar "Agenda Inteligente"): `DELETE /proyectos/{id}` devolvía 500 (mensaje genérico "No se pudo eliminar el proyecto." en el frontend). Causa real, confirmada en logs del contenedor `api`: `IntegrityError` de Postgres — al cascadear el borrado de una `Reunion`, no había ninguna relación ORM que también borrara su `Minuta` (y los `AcuerdoMinuta` colgando de ella), así que Postgres rechazaba el `DELETE FROM reuniones` por la FK `minutas_reunion_id_fkey` todavía apuntándole. Mismo problema, más chico, con `Nota` (puede colgar de un Entregable, una Reunión o una Minuta — ver `app/models/nota.py`): ningún padre tenía una relación que cascadeara hacia sus notas. **Alcance real:** no era específico de "Agenda Inteligente" ni de proyectos — el mismo bug bloqueaba también `DELETE /reuniones/{id}` (borrar una sola reunión) para cualquier reunión con minuta o notas, en cualquier proyecto. Arreglado agregando las relaciones ORM que faltaban, con `cascade="all, delete-orphan"` y `back_populates` en ambos lados (para no generar warnings de SQLAlchemy por relaciones ambiguas con `Nota.entregable`/`Nota.reunion`/`Nota.minuta`, que ya existían pero sin `back_populates`): `Reunion.minuta` (nueva, `app/models/reunion.py`), y `notas` en `Entregable`/`Reunion`/`Minuta` (`app/models/entregable.py`, `reunion.py`, `minuta.py`). `Notificacion` es la excepción a propósito — no se modeló como relación ORM porque es más una bandeja/log que un hijo propiamente dicho — sigue limpiándose a mano por query explícita en el servicio (mismo patrón que ya existía para `entregable_id`, extendido ahora también a `reunion_id`) en `eliminar_proyecto` (`app/services/proyectos.py`) y `eliminar_reunion` (`app/services/reuniones.py`, no limpiaba ninguna notificación antes). **Verificado sin arriesgar datos reales:** se corrió `eliminar_proyecto` de verdad contra "Agenda Inteligente" (que sí tiene minuta con acuerdos, notas y reuniones reales) dentro de una transacción con `db.flush()` (para forzar que Postgres valide las FKs) seguido siempre de `db.rollback()` — confirmó que el `DELETE` ya no truena, y que no se guardó nada de verdad. `docker restart` en `api` (bind mount, sin rebuild) arrancó limpio, sin warnings de SQLAlchemy. Sin cambios de frontend ni de `permissions.py`.
- ✅ Fix: la vista de David en "Tu equipo" se volvió lista plana en vez de Kanban de 3 columnas (2026-08-14, mismo día, encontrado por Yue justo tras el fix de Jasso/Diana): regresión directa de ese mismo cambio. El nuevo cálculo de `idsCubiertos` en `armarColumnasEquipo` (`utils/equipoSupervisores.js`) marcaba como "ya cubiertos" a los reportes de **TODOS** los supervisores encontrados, incluyendo el propio grupo del viewer — para David (que supervisa a Ana/Iván/Juan), eso hacía que el bloque `if (propios)` (el que promueve a cada reporte a su PROPIA columna) los viera como "ya cubiertos" y no los promoviera; como ninguna otra columna se generaba, todo caía al fallback final de "una sola columna con el propio usuario y sus proyectos" — de ahí que David viera una sola tarjeta con su nombre y sus 3 proyectos apilados adentro, en vez de 3 columnas Ana/Iván/Juan. Corregido separando la cobertura en dos momentos: primero solo se marcan como cubiertos los reportes de supervisores **distintos** al viewer (para no duplicar a alguien que ya tiene columna propia por supervisar a alguien más, el caso original que si justificaba esta lógica), y **después**, conforme el bloque `if (propios)` va promoviendo cada reporte a su columna, recién ahí se marca cada uno como cubierto — así el fallback "por persona sin relación de supervisión" (Jasso/Diana) solo actúa sobre quien de verdad quedó sin ninguna columna, sin pisar la promoción de los reportes directos del viewer. Verificado: David vuelve a ver 3 columnas (Ana/Iván/Juan); Bernardo sigue viendo David/Jasso/Diana sin cambio. `npm run build` sin errores, desplegado (`docker compose build web && up -d web`).
- ✅ "Tu equipo" ya no depende 100% de tener proyectos activos (2026-08-14, mismo día): al borrar "Agenda Inteligente" de verdad (probando el fix de arriba), Jasso y Juan José se quedaron sin ningún proyecto — y como "Tu equipo" se construye únicamente a partir de participación real en proyectos, Jasso desapareció del todo del Kanban de Bernardo (no era un bug de la vista: la fuente de datos no tenía de dónde sacarlo). Yue confirmó, ante dos opciones, la de fondo: que "Tu equipo" también use la plantilla personal "Mi equipo" (`EquipoMiembro`/`equipo_miembros`, ya existente para aplicar rápido a un proyecto) como respaldo. `resumen_equipo_multiproyecto` (`app/services/equipo_resumen.py`) ahora, después de construir la lista igual que siempre a partir de proyectos visibles, agrega también a quien esté en la plantilla del que ve la pantalla (`propietario_id == usuario.id`) y todavía no haya aparecido — con `proyectos=[]`. **No es una regla de permisos nueva**: no se toca `permissions.py` ni se otorga visibilidad extra, solo se agrega la identidad (nombre/puesto); si la persona sí tiene proyectos visibles, se calculan exactamente igual que siempre. Frontend: `KanbanSupervisores.jsx` gana un mensaje "Sin proyectos asignados todavía." para el caso de una columna con `proyectos.length === 0` (antes se habría visto en blanco). Confirmado con datos reales (sin HTTP, llamando el servicio directo): Bernardo ya tenía guardados a David/Diana/Jasso en su plantilla y David a Ana/Iván/Juan en la suya — por eso el fix funciona sin cargar nada nuevo; Jasso ahora aparece como columna propia vacía. Esto también deja documentado, para la próxima vez: la única relación "esta persona es de mi equipo" independiente de proyectos que existe en el sistema es la plantilla personal — si alguien nuevo debe aparecer en "Tu equipo" sin tener aún ningún proyecto, hay que darlo de alta ahí (`POST /mi-equipo`) además de (o en vez de) asignarlo a un proyecto. `npm run build` sin errores; `docker restart` en `api` (bind mount) arrancó limpio; desplegado (`docker compose build web && up -d web`).
- ✅ Nombre de proyecto clickeable en "Tu equipo" (2026-08-14, mismo día): Yue notó que en el Kanban "Tu equipo" (`KanbanSupervisores.jsx`), el encabezado de cada proyecto dentro de una columna (ej. "Cubo" bajo Ana) era texto plano — para entrar al proyecto había que ir a la pestaña "Proyectos" y buscarlo ahí, distinto a los entregables individuales, que sí ya tenían link. Se envolvió el nombre del proyecto en un `Link` a `/proyectos/{id}` (mismo patrón ya usado ahí mismo para los entregables). Cambio de una línea, sin tocar backend. `npm run build` sin errores, desplegado (`docker compose build web && up -d web`).
- ✅ Ronda de mejoras de UX (2026-08-14, sesión aparte, con plan mode): Yue pidió una auditoría honesta de toda la interfaz (excepto el asistente de voz, revisado aparte). Se lanzaron 3 revisiones en paralelo (navegación/flujos, formularios/estados de feedback, consistencia visual/responsive) sobre el código real, ~28 hallazgos en total. Yue eligió atacar tres bloques:
  1. **Huecos reales:**
     - **Eliminar un entregable** (no existía en ningún lado): `DELETE /entregables/{id}` nuevo (`app/routers/entregables.py` + `eliminar_entregable` en `app/services/entregables.py`, mismo gate que `actualizar_entregable` — N1/N2, no el responsable). `Notificacion.entregable_id` se limpia; `AcuerdoMinuta.entregable_id` se pone a `NULL` en vez de bloquear el borrado (el acuerdo sigue existiendo como registro de lo discutido, solo se desvincula). Botón "Eliminar" en `FormularioEntregable.jsx` (solo en edición, gateado por `puedeAsignarAOtros`).
     - **Dos borrados que fallaban en silencio** (`ModalEquipo.handleQuitar`, `ModalMinuta.handleEliminarAcuerdo`): ahora con try/catch + estado de carga, mismo patrón que `ModalReunion.handleEliminar`.
     - **Badge "Sensible" ilegible en tema oscuro** (único color de todo el sistema sin override de tema): nuevos tokens `--color-sensible`/`--color-sensible-bg` en `tokens.css` (claro y oscuro).
     - **Deep-link a entregable/reunión desde "Tu equipo"** (antes solo enlazaba al proyecto, dejando al usuario en la vista Tabla por defecto): `KanbanSupervisores.jsx` ahora linkea a `/proyectos/{id}?entregable={id}` o `?reunion={id}`; `TableroProyecto.jsx` lee ese query param después de cargar sus datos y abre el modal correspondiente directo, limpiando el query param después.
     - **Estados vacíos como invitación a actuar** (antes solo "no hay nada"), en `SeccionNotas.jsx`, acuerdos de `ModalMinuta.jsx`, `Proyectos.jsx` y `Equipo.jsx` (plantilla) — no se tocaron los estados vacíos de contenido ajeno (ej. "Sin proyectos asignados todavía" de otra persona en el Kanban), ahí no hay ninguna acción que el viewer pueda tomar.
  2. **"Mis pendientes" accionable** (antes de solo lectura): cada tarjeta abre `FormularioEntregable.jsx` en el lugar, cargando de forma perezosa el equipo de ESE proyecto (`proyectosApi.equipo`) solo al hacer clic — mismo patrón lazy-load que `KanbanMisProyectos.jsx`. `puedeAsignarAOtros` se calcula igual que en `TableroProyecto.jsx` (rol N1/N2 del usuario en el proyecto específico del entregable).
  3. **Consistencia de interacción:**
     - Nuevo `ConfirmDialog.jsx` (envuelve `Modal.jsx`) reemplazando `window.confirm()` nativo en los 7 sitios que lo usaban (`ModalReunion`, `ModalMinuta`, `ModalEquipo`, `SeccionNotas`, `Proyectos.jsx`, `Equipo.jsx`, y el nuevo botón de eliminar entregable).
     - **Escape cierra cualquier modal**: un solo cambio en `Modal.jsx` (wrapper compartido por todos), con una pila de módulo (`pilaModales`) para que Escape solo cierre el modal de hasta arriba cuando hay dos abiertos a la vez (ej. un `ConfirmDialog` sobre un formulario) — sin esto, Escape habría cerrado ambos de golpe.
     - **"Crear proyecto" unificado al patrón modal**: `ModalEditarProyecto.jsx` ahora acepta `proyecto` opcional — sin él entra en modo creación (sin el checkbox "activo", llama a `proyectosApi.crear`). `Proyectos.jsx` perdió su acordeón inline y su estado asociado; ahora usa el mismo patrón `modalProyecto: null | "nuevo" | proyecto` que ya usaba `TableroProyecto.jsx` para entregables/reuniones.

  Los hallazgos de higiene de código puro (tokens de espaciado sin usar, botones/badges chicos duplicados, hex mágico `#3a5b70`) quedaron fuera de esta ronda a propósito, Yue no los priorizó.

  **Verificación:** `npm run build` sin errores; se probó de verdad el borrado de un entregable (crear uno de prueba en un proyecto real, eliminarlo con el servicio nuevo, confirmar que desaparece) dentro del contenedor `api`, sin dejar nada de prueba en la base; `docker restart` en `api` arrancó limpio, sin warnings de SQLAlchemy; desplegado (`docker compose build web && up -d web`); confirmado 200 en la URL pública tanto de la API como del frontend tras el despliegue.
- ⬜ **Pendiente (2026-08-10, sin fecha):** hoy toda `Reunion` requiere `proyecto_id` (obligatorio en el modelo) — no existe el concepto de reunión "general"/sin proyecto ni multi-proyecto. Si alguien quiere agendar algo tipo "ver avances generales con mi equipo" (no ligado a un solo proyecto), hoy no se puede — ni desde la UI normal ni desde el asistente de voz, que por eso siempre pregunta a qué proyecto pertenece. Workaround actual: agendarla dentro de cualquier proyecto existente, o crear un proyecto ligero tipo "Seguimiento general" solo para agrupar esas reuniones. Si se decide construir esto de verdad, implica cambiar `proyecto_id` a opcional y sobre todo **redefinir la regla de visibilidad de reuniones** (¿quién ve una reunión sin proyecto?) — es justo el tipo de cambio de permisos que hay que acordar con Yue/cliente antes de tocar, no un ajuste chico. Confirmado explícitamente con Yue: se deja como pendiente, no se construye por ahora.
- ⬜ Fase 4: carga real de datos de prueba (ya existe `seed.py`, y `seed_usuarios_reales.py`/`reset_passwords_reales.py` para dar de alta a los 7 usuarios reales del cliente sin crear proyectos/roles) + demo formal al cliente. Sigue siendo lo único pendiente del roadmap original. Nota: `seed.py` tiene un typo pre-existente (línea de `usuarios_data`, "Lider Proyecto A" apunta a `n1@demo.com` en vez de `n2a@demo.com`) que hace que el script falle — no se tocó porque no es parte de esta tarea, pero hay que arreglarlo antes de usarlo.

Antes de avanzar a algo fuera de esta lista, confirma con Yue.
